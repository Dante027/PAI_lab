<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HomePantry.Views.ProductDetailPage"
             Title="Szczegóły produktu">

    <ScrollView>
        <VerticalStackLayout>
            <Frame CornerRadius="12"
                   Margin="15"
                   Padding="20"
                   HasShadow="True"
                   BorderColor="{StaticResource PantryGreen}">
                <Grid RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto"
                      RowSpacing="10">
                    <Label Grid.Row="0" x:Name="lblNazwa"
                           FontSize="22" FontAttributes="Bold" />
                    <Label Grid.Row="1" x:Name="lblKategoria" />
                    <Label Grid.Row="2" x:Name="lblIlosc" />
                    <Label Grid.Row="3" x:Name="lblWaznosc" />
                    <Label Grid.Row="4" x:Name="lblStatus" />
                    <Label Grid.Row="5" x:Name="lblUwagi" LineBreakMode="WordWrap" />
                </Grid>
            </Frame>

            <Button Text="Powrót do kategorii"
                    Margin="15,0,15,20"
                    Clicked="OnBackClicked" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
